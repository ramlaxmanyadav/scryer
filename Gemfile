# frozen_string_literal: true

source "https://rubygems.org"

# Pulls in the gemspec's own development dependencies (currently just rake,
# for `rake test`/`rake benchmark`) — no separate list to keep in sync here.
gemspec
